# Home Day Swipe TODO

## Goal

Users can horizontally swipe the Home foredrop timeline to move to the previous or next day. The entire Home day surface updates together: date chrome, task timeline, calendar events, habit rows, mixed agenda rows, empty states, and Back to Today visibility.

## Grounding

- PRD: Home is Tasker's single-glance day command center combining tasks, fixed commitments, routines, busy blocks, open gaps, and guidance.
- Calendar docs: Home and timeline should consume the same resolved calendar state; calendar data is read-only and must refresh when the selected day changes.
- Habit docs: Home habit rows are first-class day projections, and visual/computed habit state must not diverge.
- Current UI: `HomeForedropView` hosts the timeline surface and already calls `viewModel.selectDate(_:)` from the week strip.
- Current state: `HomeViewModel.selectedDate`, `HomeChromeSnapshot.selectedDate`, `HomeCalendarSnapshot.selectedDate`, and `HomeTimelineSnapshot.selectedDate` already exist.
- Current top chrome: `HomeChromeSnapshot+Presentation` already shows Back to Today for non-today custom dates.
- Current performance path: `HomeRenderTransaction` commits chrome/tasks/habits/calendar/overlay slices separately through store objects.

## Implementation Plan

- [x] Add a day-navigation API on `HomeViewModel`.
  - [x] Normalize all incoming dates to `Calendar.current.startOfDay(for:)`.
  - [x] Add `shiftSelectedDay(by offset: Int, source: HomeDateNavigationSource)` and `returnToToday(source:)`.
  - [x] Keep `selectDate(_:)` as the shared path used by date picker, week strip, replan placement, and swipe.
  - [x] Avoid double work when the target day is already selected.

- [x] Make selected-day updates atomic enough for smooth rendering.
  - [x] Wrap date, scope, quick-view reset, and visible reload scheduling in `performHomeRenderStateBatch`.
  - [x] Ensure `activeScope == .customDate(targetDay)` for non-today days and `.today` for today.
  - [x] Keep Back to Today driven by `HomeChromeSnapshot.shouldShowBackToToday`.
  - [x] Do not reset filters unrelated to day navigation unless they conflict with the Today timeline surface.

- [x] Add a horizontal swipe gesture to the foredrop timeline area.
  - [x] Attach the gesture at the `todayTimelineSurface` / `TimelineForedropView` boundary so it covers the whole foredrop day content, not only the handle.
  - [x] Gate recognition by axis: only handle when absolute horizontal translation clearly exceeds vertical translation.
  - [x] Ignore swipes while search/analytics faces are active, while a replan card/dock is actively dragging, or when VoiceOver escape/scroll accessibility actions should own navigation.
  - [x] Keep the existing vertical `TimelineForedropBar` drag path unchanged.
  - [x] Prefer SwiftUI `DragGesture`; use a UIKit bridge only if ScrollView gesture arbitration proves unreliable on device.

- [x] Add swipe animation without rebuilding the full Home shell.
  - [x] Store a transient `daySwipeDirection` / `daySwipePhase` in `HomeForedropView` or `HomeTimelineViewModel`.
  - [x] Animate the day content with a short asymmetric horizontal transition keyed by selected day.
  - [x] Use reduced-motion fallback: crossfade or no slide when `accessibilityReduceMotion` is true.
  - [x] Preserve structural identity for the Home shell, top chrome, bottom bar, and persistent faces.
  - [x] Avoid `AnyView` or conditional branches around the full foredrop surface beyond existing architecture.

- [x] Ensure all plotted data follows the selected day.
  - [x] Tasks: verify `GetHomeFilteredTasksUseCase` receives `.customDate(targetDay)` and uses `HomeListScope.referenceDate`.
  - [x] Timeline: verify `buildTimelineSnapshot` uses the normalized selected day for anchor window, task filtering, week summary, all-day items, gaps, and current-time behavior.
  - [x] Calendar: verify `CalendarIntegrationService.refreshContext(referenceDate:)` runs on selected-day changes and `HomeCalendarSnapshot` filters `eventsInRange` to the selected day.
  - [x] Habits: verify `BuildHabitHomeProjectionUseCase.execute(date:)`, library/history rows, and quiet tracking rows use the same target day captured for that reload.
  - [x] Daily reflection/plan cards: verify the selected-day draft and reflection entry change with the day or are intentionally hidden for non-today if product decides so.

- [x] Add fast neighbor-day prefetching.
  - [x] After a selected-day load completes, warm previous and next day task projections at utility priority.
  - [x] Cache by normalized day plus data revision/preferences revision.
  - [x] Never pass Core Data managed objects across queues; cache value snapshots only.
  - [x] Cancel or deprioritize stale prefetch when the user swipes again.
  - [x] Keep visible-day correctness ahead of prefetch completeness.
  - [ ] Consider warming calendar/habit projections separately if profiling shows task-cache prefetch is not enough.

- [x] Protect against async stale writes.
  - [x] Capture `targetDay` before habit/calendar/task async calls and discard results if `selectedDate` no longer matches.
  - [x] Extend existing generation checks to day-specific reload work.
  - [x] Audit `refreshDueTodayAgenda` for direct reads of `self.selectedDate` inside nested async completions; replace with captured normalized day.

- [x] Instrument performance.
  - [x] Add trace events for `HomeDaySwipeStarted`, `HomeDaySwipeCommitted`, first visible transaction, and stale reload drops.
  - [x] Track cache hit/miss for adjacent-day prefetch.
  - [x] Target: chrome updates immediately, cached neighbor day presents in one frame budget after gesture end, uncached day preserves current usable snapshot while loading.

- [ ] Add tests.
  - [x] Unit: swipe resolver maps left/right/vertical/short/predicted flick gestures.
  - [x] Unit: `HomeViewModel.shiftSelectedDay` changes scope/date correctly and Back to Today appears for yesterday/tomorrow.
  - [ ] Unit: custom-day task projection returns selected-day tasks, overdue carry-over, completed inline rows, and no unrelated future tasks.
  - [ ] Unit: habit projection uses the target day captured at reload time, not the latest `selectedDate`.
  - [ ] Unit: calendar snapshot filters selected-day timed/all-day events and does not keep today's event count as the selected-day count where UI needs selected-day data.
  - [ ] Unit: stale generation/day reloads are dropped after rapid left-right swipes.
  - [x] UI: swipe left moves to the previous day, date label changes, Back to Today appears, timeline identifiers change.
  - [x] UI: swipe right moves to the next day, habits/calendar/task rows change, Back to Today returns to today.
  - [ ] UI/accessibility: Reduce Motion path does not slide; date picker/week-strip paths still work.

## Verification

- [x] `xcodebuild -workspace Tasker.xcworkspace -scheme "To Do List" -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build`
- [x] `git diff --check`
- [ ] `./taskerctl build` is blocked by CocoaPods target lookup: `TaskerTests` is not present in `Tasker.xcodeproj`.
- [ ] Targeted `xcodebuild ... test` is blocked because `To_Do_ListTests` and `To_Do_ListUITests` are not members of the active scheme/test plan.
- [ ] Direct `To Do ListTests` target build is blocked in the MLX package while linking `Cmlx` for x86_64.

## Follow-up: Reversed Swipe Direction + Custom-Day Header Date

- [x] Reverse physical swipe mapping: left swipe moves to next day, right swipe moves to previous day.
- [x] Keep semantic accessibility actions unchanged for Previous Day and Next Day.
- [x] Reverse the day-content transition edges so animation follows the new physical swipe direction.
- [x] Keep custom-day date/month visible in top chrome even when Back to Today makes the leading controls wider.
- [x] Update resolver and UI test expectations for the reversed physical direction.

## Risks

- `refreshDueTodayAgenda` currently uses `self.selectedDate` inside async work; rapid swipes can apply the wrong habit projection unless the target day is captured.
- `GetHomeFilteredTasksUseCase` already caches by encoded `HomeListScope`, but adjacent-day prefetch should not bloat the unbounded result cache.
- Calendar snapshot currently exposes `eventsTodayCount`; selected-day UI should use selected-day counts where applicable.
- A full-surface `DragGesture` can fight vertical `ScrollView` gestures unless axis-gated carefully.
- The current data stack in this repo is Core Data repositories, not SwiftData. SwiftData constraints still apply if/when this feature is ported, but this implementation should follow the existing Core Data read-model contracts.

## Acceptance Criteria

- Swiping left on the Home foredrop changes to the next day; swiping right changes to the previous day.
- Date chrome, timeline tasks, calendar events, habits, mixed agenda rows, and empty states all reflect the selected day.
- Date and month chrome remain visible for yesterday, tomorrow, and any custom day.
- Back to Today appears after any non-today swipe and returns the surface to today.
- Existing week-strip date selection and date picker behavior still work.
- Vertical timeline scrolling and the foredrop reveal handle still feel natural.
- Rapid repeated swipes do not show stale habit/calendar/task data from an intermediate day.
- Reduce Motion users do not receive a large horizontal slide.
