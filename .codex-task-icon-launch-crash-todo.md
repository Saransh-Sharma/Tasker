# Task Icon Launch Crash TODO

- [x] Harden snapshot mapping to avoid undefined-key crashes.
- [x] Add new Core Data model version with `TaskDefinition.iconSymbolName`.
- [x] Move `.xccurrentversion` to the new model version.
- [x] Wire `TaskModelV3_TaskIcons.xcdatamodel` into `Tasker.xcodeproj` as current version.
- [x] Require `TaskDefinition.iconSymbolName` in runtime schema validation.
- [x] Make preflight migration target the current compiled model (not hard-coded WeeklyPlanning).
- [x] Add crash regression + migration + persistence tests for task icon mapping.
- [x] Run targeted tests (blocked by existing missing local modules: `FirebaseCore`, `MaterialComponents`, `DGCharts`, `ViewAnimator`).
- [x] Validate Core Data model compilation directly via `momc`.
