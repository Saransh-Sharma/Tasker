# Calendar/Schedule Remediation TODO

- [x] Add new calendar module/public interface types (`allDayOnly`, iPad `schedule`, schedule presentation mode, deep-link notifications)
- [x] Harden permission recovery and write-only action routing to Settings
- [x] Add initial-loading states in schedule and chooser while preserving refresh content
- [x] Localize/normalize calendar and schedule copy
- [x] Implement iPad schedule embedded parity and keep iPhone modal flow
- [x] Add calendar deep-link routing (`tasker://calendar/schedule`, `tasker://calendar/chooser`)
- [x] Add CalendarIntegrationService projection caching + invalidation
- [x] Harden preferences persistence (canonicalize selected IDs, no-op saves, backward-safe decode)
- [x] Tokenize corner radii and improve event unavailable fallback
- [x] Update tests (`HomeCalendarModuleUITests`, `HomeCalendarIntegrationTests`, `CalendarIntegrationServiceTests`, `CalendarComputationUseCasesTests` if needed)
- [ ] Run target test matrix and fix regressions

## Calendar Schedule Redesign (Wireframe Distill)

- [x] Distill schedule header into context label + segment + filters action
- [x] Remove schedule toolbar refresh and add pull-to-refresh
- [x] Remove duplicated snapshot/agenda surfaces from main schedule canvas
- [x] Add week strip with selected-day timeline rendering
- [x] Make timeline cards tappable to open native EventKit detail
- [x] Distill chooser visuals while keeping grouped source sections
- [x] Update `HomeCalendarModuleUITests` expectations for redesigned timeline/week strip
- [x] Run `CalendarIntegrationServiceTests` + `HomeCalendarIntegrationTests`
- [ ] Run `HomeCalendarModuleUITests` (blocked by unrelated `To Do ListTests` linker failure in current worktree)
