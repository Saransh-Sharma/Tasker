# Home Habit Row Tap Cycle TODO

- [x] Update `HomeHabitRowView` interaction contract (icon tap vs row tap)
- [x] Extend `HomeHabitLastCellInteraction` for `lapseOnly` tracking/lapsed cycle
- [x] Route row tap actions with source tags across Home surfaces
- [x] Add one-shot mutation feedback channel in `HomeViewModel`
- [x] Consume mutation feedback in `HomeForedropView` (toast + haptic)
- [x] Update UI tests in `HabitBoardUITests` *(updated; local seed currently does not expose any `home.habitRow.*` elements, so row-interaction cases fail at lookup)*
- [x] Update unit tests (`HomeHabitLastCellInteractionTests`, `HomeViewModelPersistenceTests`)
- [x] Run targeted tests
