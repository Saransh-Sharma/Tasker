# App Attest test fixtures

These synthetic App Attest fixtures come from `node-app-attest` 1.0.1 and are
used only for adversarial verifier tests. The upstream package and fixtures are
MIT licensed. They contain no LifeBoard credentials, user identifiers, or
production key material.

