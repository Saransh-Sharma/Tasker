# Swift files intentionally present but not referenced by LifeBoard.xcodeproj.
#
# This list is empty and should stay that way. A Swift file that no target
# compiles is not "pending wiring" — it is unreachable code that no build, test
# or guardrail can observe, and it rots silently. Wire a new file into a target
# in the same change that adds it, or do not add it.
#
# Files excluded on purpose (Core Data hand-written accessors) live in
# scripts/xcode-target-membership-exclusions.txt instead.
